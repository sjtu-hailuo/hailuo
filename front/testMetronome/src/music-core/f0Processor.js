/*
import {
  DeviceEventEmitter
} from 'react-native';

class F0Processor {
  constructor(pitchClass, hopSize) {
    this.hopSize = hopSize;
    this.pitchListener = DeviceEventEmitter.addListener('pitchFreq', (freq) => {
      this.appendPitch(pitchClass.freqToMidi(data));
    });

    this.rawPitches = [];
    this.pitches = [];
    this.noiseTag = [];
    this.smoothedPitches = [];
    this.notes = [];

    this.prevOnset = 0;
    this.prevAvrPitch = 0;
    this.prevMaxPitch = 0;
    this.prevMinPitch = 0;
    this.durationCompansation = 0;
  }

  appendPitch(newPitch) {
  }

  terminate() {
    this.pitchListener.remove();
  }
}
*/
